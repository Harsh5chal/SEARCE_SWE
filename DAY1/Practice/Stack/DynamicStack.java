public class DynamicStack extends PracticeCustomStack{
    public DynamicStack(int size){
      super(size); //it will call custom stack(int size)
    }
    
    //Just push is changed
    //we will override to use custom size
    
    @Override
    public boolean push(int item){
      if (this.isFull)
    }
  }